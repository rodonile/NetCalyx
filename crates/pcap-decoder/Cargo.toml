lints.workspace = true

[package]
name = "netcalyx-pcap-decoder"
version = "0.2.0"
edition = "2024"
authors = ["Riccardo Negri <hello@riccardonegri.dev>"]
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/network-analytics/NetCalyx"
homepage = "https://github.com/network-analytics/NetCalyx"
description = """
CLI Utility to convert NetFlow, IPFIX, BGP, BMP or UDP-Nofif data from a PCAP file to a JSON Lines file.
"""
keywords = ["pcap", "parser", "decoder", "protocol"]
categories = ["network-programming", "parsing"]
build = "build.rs"

[dependencies]
netcalyx-flow-pkt = { workspace = true, features = ["codec"] }
netcalyx-pcap-reader = { workspace = true }
netcalyx-bmp-pkt = { workspace = true, features = ["codec"] }
netcalyx-bgp-pkt = { workspace = true, features = ["codec"] }
netcalyx-udp-notif-pkt = { workspace = true, features = ["codec"] }
ciborium = { workspace = true }
serde = { workspace = true, features = ["derive"] } #  a generic serialization/deserialization framework
serde_json = { workspace = true, features = ["preserve_order"] }
pcap-parser = { workspace = true, features = ["data"] }
bytes = { workspace = true }
tokio-util = { workspace = true, default-features = false, features = ["codec"] }
clap = { workspace = true, features = ["default", "derive", "string"] }
shadow-rs = { workspace = true, features = ["default", "metadata", "build"] }

[build-dependencies]
shadow-rs = { workspace = true, default-features = true }

[dev-dependencies]
chrono = { workspace = true, default-features = false, features = [
    "std",
] }
tempfile = { workspace = true }
